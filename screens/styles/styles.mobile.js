import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
    backgroundColor: '#a8d8ea', // Android background color
  },
  text: {
    fontSize: 20,
    fontWeight: 'bold',
    color: '#2c6e91', // Android text color
  },
});
